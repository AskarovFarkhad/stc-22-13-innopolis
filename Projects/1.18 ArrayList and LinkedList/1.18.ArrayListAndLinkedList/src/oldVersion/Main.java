package oldVersion;

public class Main {

    public static void main(String[] args) {
        MyArray myArray = new MyArray();

        int i = 0;
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);
        myArray.addElement(i++);

        int j = 0;
    }
}
